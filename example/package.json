{
  "name": "creactive-example",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "prebuild": "expo prebuild",
    "test:visual:ios": "expo prebuild --platform ios && detox build -c ios && detox test -c ios",
    "test:visual:android": "expo prebuild --platform android && detox build -c android && detox test -c android",
    "test:visual:android:update": "expo prebuild --platform android && detox build -c android && detox test -c android -- -u"
  },
  "dependencies": {
    "expo": "57.0.19",
    "react": "19.2.3",
    "react-native": "0.86.3"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@jest/globals": "^29.7.0",
    "babel-plugin-module-resolver": "^5.0.2",
    "babel-preset-expo": "~57.0.10",
    "detox": "^20.51.4",
    "jest": "^29.7.0",
    "jest-circus": "^29.7.0",
    "jest-image-snapshot": "^6.4.0"
  }
}
