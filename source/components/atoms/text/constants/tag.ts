/**
 * Enumerated text tag constants.
 * Allows to control HTML tag to render.
 * Makes no sense on native platforms.
 * @see Text.Tag
 */
export enum TextTag {
  H1,
  H2,
  H3,
  H4,
  H5,
  H6,
  P,
  SPAN,
}
