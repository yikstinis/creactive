# Types

- Use `interface` for component properties and context values.
- Use `type` for `[Component]Component` to allow intersection with static properties via `&`.
- Name props interfaces `[Component]Properties` and context value interfaces
  `[Component]ContextValue`.
